.DS_Store
__pycache__/
*.py[cod]
*.egg-info/
.idea
.uv-cache
.env
.env.*
!.env.example
.venv/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
build/
dist/
automox-mcp-design-guide.md

# Upstream OpenAPI spec — local reference only, never commit
console-api.yaml

# Live-tenant probe harnesses — manual debugging, never commit (not in CI)
tests/probe_*.py

# Local OpenAPI spec exports — reference only, never commit (5+ MB dumps)
Automox Console API.json
*.openapi.json

# Local webhook reference dump — never commit
webhooks.json

# MCP Registry tooling — local only, never commit
mcp-publisher
mcp-registry-key.pem
*.pem

# MCPB Desktop Extension build artifacts — built by CI, not committed
*.mcpb
mcpb/.venv/
mcpb/uv.lock
.claude/
